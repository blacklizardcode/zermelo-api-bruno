info:
  name: users/me
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v3/users/~me/"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
