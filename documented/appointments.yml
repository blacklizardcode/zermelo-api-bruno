info:
  name: appointments
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v3/appointments"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: start
      value: "1787868000"
      type: query
      disabled: true
    - name: end
      value: "1787950800"
      type: query
      disabled: true
    - name: user
      value: ~me
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
