info:
  name: branchesofschools
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v3/branchesofschools"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: schoolYear
      value: "2026"
      type: query
      disabled: true
    - name: archived
      value: "false"
      type: query
      disabled: true
    - name: branch
      value: 01kl
      type: query
      disabled: true
    - name: id
      value: "502"
      type: query
      description: seems to be same as schoolInSchoolYear
      disabled: true
    - name: schoolInSchoolYear
      value: "502"
      type: query
      description: seems to be same as id
      disabled: true
    - name: fields
      value: id,schoolInSchoolYear,branch,name,schoolYear
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
